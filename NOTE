HAL_GPIO_Callback(uint16_t GPIO_PIN)
{
	HAL_GPIO_TogglePin(GPIOA, GPIO_PIN_0);
}

 while (1)
  {
    /* USER CODE END WHILE */
	  	  HAL_GPIO_WritePin(GPIOA, GPIO_PIN_5, GPIO_PIN_SET);
		  HAL_Delay(1000);
		  HAL_GPIO_WritePin(GPIOA, GPIO_PIN_5, GPIO_PIN_RESET);
		  HAL_Delay(1000);
    /* USER CODE BEGIN 3 */
  }

EXTI PIN DECLAIRING 
GPIO
NVIC
RCC
A0=P0 PIN 0 

//NUCLEO-F446RE

UART COMMUNICATION
SIMPLEX-ONE

START BIT,DATA BIT,PARITY BIT,STOP BIT
TRASMISSION IN 8BITS
START-TX

PARITY - ERROR DETECTIONB
EVEN PARITY ODD PARITY
HALF DUPLEX-TURNS
FULL DUPLEX-TX AND RX

STOP BIT- LOW TO HIGH

BOUD RATE - BITS PER SEC   

SPRINT IS USED TO FORMAT AND STORE A STING TO VARIABLE LIKE HELLO AND 10 
 ADC- clock,adc enable,uart(for reaing), 
  while (1)
  {
    /* USER CODE END WHILE */
	  HAL_ADC_Start(&hadc1);
	  HAL_ADC_PollForConversion(&hadc1,100);
	  adc_data = HAL_ADC_GetValue(&hadc1);
	  sprintf(Uart_msg, "ADC Value : %lu\n", adc_data);
	  HAL_UART_Transmit(&huart2,(uint8_t*)Uart_msg , strlen(Uart_msg),100);
	  HAL_Delay(500);
    /* USER CODE BEGIN 3 */
  }
  /* USER
ABOVE MAIN DECLAIRATION 

/* USER CODE BEGIN 4 */
void HAL_ADC_ConvCpltCallback(ADC_HandleTypedef *hadc)
{
	adc_data = HAL_ADC_GetValue(&hadc1);
}
/* USER CODE END 4 */
